*   Populate `test_prompt_builder.py` with the `unittest` code provided earlier.
*   Create `test_ai_manager.py`. Testing this effectively requires mocking the `openai` client library to avoid making actual API calls during tests. Use `unittest.mock` or `pytest-mock`.
